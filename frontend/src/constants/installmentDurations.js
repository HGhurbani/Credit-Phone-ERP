/** Preset installment lengths (months) shown as quick options in UI */
export const INSTALLMENT_DURATION_PRESETS = [3, 4, 5, 6, 12];

/** Must stay in sync with `StoreContractRequest` duration_months max */
export const INSTALLMENT_DURATION_MAX_MONTHS = 60;
